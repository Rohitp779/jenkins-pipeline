pipeline {
        agent { node { label 'built-in'
        customWorkspace('/home/')
        } }

        stages {  
          stage('Built')
          {
              steps{
                    echo 'i have created and typed built succesfully'
                    sh 'pwd'
                    }
                }
            }
        }

